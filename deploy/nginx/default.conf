# ============================================
# Nginx Configuration for 双人岛 (Lovers Plan)
# ============================================
# 注意：
# 1. 当前使用 HTTP，微信小程序正式发布必须配置 HTTPS
# 2. 准备好域名后，用 certbot 申请 SSL 证书
# 3. 取消下面 HTTPS server 部分的注释并替换域名

upstream app_backend {
    server app:8080;
}

upstream minio_api {
    server minio:9000;
}

# ---- HTTP (临时用于开发调试) ----
server {
    listen 80;
    server_name _;

    # 后端 API 代理
    location /api/ {
        proxy_pass http://app_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # MinIO 文件（图片等静态资源）
    location /minio/ {
        proxy_pass http://minio_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        client_max_body_size 50M;
    }

    location / {
        return 404;
    }
}

# =====================================================================
# HTTPS — 准备好域名后启用（需要 SSL 证书）
# =====================================================================
# 步骤：
#   1. 将域名 DNS 指向 47.93.25.125
#   2. 安装 certbot: apt install certbot python3-certbot-nginx
#   3. 执行: certbot --nginx -d yourdomain.com
#   4. 取消下面注释，替换 yourdomain.com
#   5. 重启: docker restart lp-nginx
# =====================================================================
#server {
#    listen 443 ssl http2;
#    server_name yourdomain.com;
#
#    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#    ssl_protocols TLSv1.2 TLSv1.3;
#    ssl_ciphers HIGH:!aNULL:!MD5;
#
#    # 后端 API
#    location /api/ {
#        proxy_pass http://app_backend;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#        proxy_read_timeout 60s;
#    }
#
#    # MinIO 文件访问
#    location /minio/ {
#        proxy_pass http://minio_api;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#        proxy_read_timeout 120s;
#        client_max_body_size 50M;
#    }
#
#    location / {
#        return 404;
#    }
#}
#
#server {
#    listen 80;
#    server_name yourdomain.com;
#    return 301 https://$server_name$request_uri;
#}
